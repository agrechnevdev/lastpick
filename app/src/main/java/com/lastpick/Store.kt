package com.lastpick


class Store<A, S>(
//    private val reducer: Reducer<S, A>,
//    private val middlewares: List<Middleware<A, S>>,
//    private val initialState: S
) {
//    private val state = BehaviorSubject.createDefault(State())
//    private val actions = PublishSubject.create<Action>()
//    fun wire(): Disposable {
//        val disposable = CompositeDisposable()
//        disposable += actions.withLatestFrom(state) { action, state ->
//
//        }
//    }
//
//    fun bind(view: MviView<A, S>): Disposable {
//        val disposable = CompositeDisposable()
//        disposable +=
//    }
}